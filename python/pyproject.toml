[project]
name = "my-algorithm-lab-python"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "pyright[node,nodejs]>=1.1.404",
]

[tool.pytest.ini_options]
python_files = ["test_*.py"]
testpaths = ["."]
